
const electron = require('electron')
console.log(process.getSystemVersion())
// var x = parseInt( process.getSystemMemoryInfo().total / 1024 / 1024);

// document.getElementById('memory').innerHTML = x